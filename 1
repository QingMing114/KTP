用户输入 "帮我分析这张TIF影像的LAI"
    │
    ▼
┌─────────────────────────────────────────────────────┐
│  第一步：快速判断（不走大模型）                        │
│  engine.py L466: _should_use_direct_chat_reply()     │
│                                                      │
│  如果是闲聊（"你好"、"什么是NDVI"）→ 直接走轻量路径    │
│  _generate_direct_chat_reply() → 一次LLM调用 → 返回   │
│                                                      │
│  如果包含分析关键词（"分析"、"检测"、".tif"等）        │
│  → 进入下面的 Agent Loop                             │
└─────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────┐
│  Agent Loop（最多 20~30 步循环）                      │
│  engine.py L465: for step_index in range(max_steps)  │
│                                                      │
│  ┌─────────────────────────────────────────────┐     │
│  │ 第二步：大模型决策（Planner）                  │     │
│  │ engine.py L568: planner.plan()               │     │
│  │                                              │     │
│  │ 大模型看到：用户消息 + 工具列表 + 历史记录      │     │
│  │ 大模型输出三种决策之一：                       │     │
│  │   • reply    → 直接回复，循环结束              │     │
│  │   • call_tool → 调用工具，继续循环             │     │
│  │   • fail     → 报错退出                       │     │
│  └─────────────────────────────────────────────┘     │
│      │                                                │
│      │ 如果是 call_tool                               │
│      ▼                                                │
│  ┌─────────────────────────────────────────────┐     │
│  │ 第三步：执行工具                               │     │
│  │ engine.py L750-759: _execute_single_tool_call │     │
│  │                                              │     │
│  │ registry.invoke(tool_name, tool_input)        │     │
│  │   → ktp.analysis_pipeline → 斑秃识别          │     │
│  │   → prosail.invert_lai_tif → LAI反演          │     │
│  │   → knowledge.search_local → 知识检索          │     │
│  │   → ...                                      │     │
│  │                                              │     │
│  │ 工具返回 (Observation, Artifacts)             │     │
│  └─────────────────────────────────────────────┘     │
│      │                                                │
│      │ 工具结果加入 tool_history                       │
│      ▼                                                │
│  ┌─────────────────────────────────────────────┐     │
│  │ 第四步：重复调用检测                            │     │
│  │ engine.py L817-876                            │     │
│  │                                              │     │
│  │ 如果连续2次调用完全相同的工具 → 自动终止循环     │     │
│  └─────────────────────────────────────────────┘     │
│      │                                                │
│      │ 回到第二步，大模型再次决策                      │
│      │ （这次大模型能看到工具返回的结果了）             │
│      ▼                                                │
│  ┌─────────────────────────────────────────────┐     │
│  │ 第五步：大模型再次决策                          │     │
│  │                                              │     │
│  │ 大模型看到：用户消息 + 工具列表 + 历史记录      │     │
│  │            + 上一步工具返回的结果               │     │
│  │                                              │     │
│  │ 这次大模型可能：                               │     │
│  │   • reply → "根据分析结果，LAI值为3.2..."      │     │
│  │   • call_tool → 再调一个工具（如生成报告）      │     │
│  └─────────────────────────────────────────────┘     │
│                                                      │
│  直到 reply 或达到最大步数                             │
└─────────────────────────────────────────────────────┘
    │
    ▼
  SSE 流式推送给前端
  前端实时展示：思考过程 → 工具调用 → 工具结果 → 最终回复